{
  "name": "text-to-dash-figma-plugin",
  "version": "1.0.0",
  "description": "Figma plugin to convert text layers into dashed vector path strokes",
  "main": "dist/code.js",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.96.0",
    "esbuild": "^0.20.2",
    "typescript": "^5.4.5"
  }
}
